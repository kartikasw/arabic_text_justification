# Consumers use the prebuilt native artifacts:
#   android/src/main/jniLibs/<abi>/libarabic_text_justification.so
#   ios/Frameworks/arabic_text_justification.xcframework
# They never compile harfbuzz/freetype, so the source trees and native
# build scaffolding are stripped from the published package.

third_party/
native/
scripts/
.build/
example/
