import 'package:flutter/material.dart';

const ayahMarker = '۝';

const wordDuration = Duration(milliseconds: 500);

const appGreen = Color(0xFF2E7D32);

const linePadding = EdgeInsets.symmetric(vertical: 2);

const scrollablePagePadding = EdgeInsets.fromLTRB(8, 12, 8, 0);
